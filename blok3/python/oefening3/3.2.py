# import what you need
import numpy as np
import matplotlib.pyplot as plt
import scipy as sp

#a
# som van alle elementen = np.sum([])
# gemiddelde van alle elementen = np.mean([])
# standaarddeviatie = np.std([])
# en, of, niet = &, |, ~

plt.plot()